@model SmartHealthcare.Models.DTOs.UpdateBillDTO
@{
    ViewData["Title"] = "Edit Bill";
    var appointment = ViewBag.Appointment as SmartHealthcare.Models.DTOs.AppointmentDTO;
}

<div class="container-fluid mt-4">

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h4 class="mb-0">Edit Bill</h4>
            <small class="text-muted">Update billing information</small>
        </div>

        <a href="@Url.Action("Details", "Appointment", new { id = appointment?.Id })"
           class="btn btn-outline-dark btn-sm">
            Back
        </a>
    </div>

    <!-- APPOINTMENT INFO -->
    <div class="card border-0 shadow-sm mb-4">
        <div class="card-body p-0 table-responsive">

            <table class="table mb-0">
                <tbody>
                    <tr>
                        <th>Appointment ID</th>
                        <td>@appointment?.Id</td>
                    </tr>

                    <tr>
                        <th>Patient</th>
                        <td>@appointment?.PatientName</td>
                    </tr>

                    <tr>
                        <th>Doctor</th>
                        <td>Dr. @appointment?.DoctorName</td>
                    </tr>
                </tbody>
            </table>

        </div>
    </div>

    <!-- EDIT BILL FORM (TABLE STYLE) -->
    <form asp-action="Edit" method="post">

        <div class="card border-0 shadow-sm mb-4">
            <div class="card-body p-0 table-responsive">

                <table class="table mb-0">
                    <tbody>

                        <!-- Consultation Fee -->
                        <tr>
                            <th style="width:250px;">Consultation Fee</th>
                            <td>
                                <div class="input-group">
                                    <span class="input-group-text">₹</span>
                                    <input asp-for="ConsultationFee"
                                           type="number"
                                           step="0.01"
                                           min="0"
                                           class="form-control"
                                           placeholder="Enter consultation fee" />
                                </div>
                                <span asp-validation-for="ConsultationFee" class="text-danger small"></span>
                            </td>
                        </tr>

                        <!-- Medicine Charges -->
                        <tr>
                            <th>Medicine Charges</th>
                            <td>
                                <div class="input-group">
                                    <span class="input-group-text">₹</span>
                                    <input asp-for="MedicineCharges"
                                           type="number"
                                           step="0.01"
                                           min="0"
                                           class="form-control"
                                           placeholder="Enter medicine charges" />
                                </div>
                                <span asp-validation-for="MedicineCharges" class="text-danger small"></span>
                            </td>
                        </tr>

                        <!-- Payment Status -->
                        <tr>
                            <th>Payment Status</th>
                            <td>
                                <select asp-for="PaymentStatus" class="form-select">
                                    <option value="Unpaid">Unpaid</option>
                                    <option value="Paid">Paid</option>
                                </select>
                                <span asp-validation-for="PaymentStatus" class="text-danger small"></span>
                            </td>
                        </tr>

                    </tbody>
                </table>

            </div>
        </div>

        <!-- ACTIONS -->
        <div class="d-flex gap-2">

            <button type="submit" class="btn btn-dark btn-sm px-4">
                Update Bill
            </button>

            <a href="@Url.Action("Details", "Appointment", new { id = appointment?.Id })"
               class="btn btn-outline-secondary btn-sm px-4">
                Cancel
            </a>

        </div>

    </form>

</div>